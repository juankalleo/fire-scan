{
  "name": "firescan-root",
  "version": "0.1.0",
  "private": true,
  "description": "FireScan - Gerenciador de Mangás para Windows",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build",
    "ui:dev": "npm run dev --prefix src-ui",
    "ui:build": "npm run build --prefix src-ui",
    "rust:build": "cargo build --release --manifest-path src-tauri/Cargo.toml"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^1.5.3",
    "@types/node": "^25.3.5"
  }
}
